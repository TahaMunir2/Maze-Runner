# These are the files for the camera encoding algorithm:
We only keep the pixels on every even column and row (reduces size by 75%), instead of just throwing pixels away though the ones we keep are averaged with the pixel right before it to give a smoothed out output.
